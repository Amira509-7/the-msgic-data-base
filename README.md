# the-msgic-data-base
A Machine Learning classification project using the MAGIC Gamma Telescope dataset. The project compares KNN, Naive Bayes, Logistic Regression, SVM, and a Neural Network with data preprocessing, hyperparameter tuning, and model evaluation.
